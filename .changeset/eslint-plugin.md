---
"@sorostream/eslint-plugin": minor
---

Add `@sorostream/eslint-plugin` (issue #218) with three rules: `no-withdraw-without-claimable-check`, `await-async-sdk-methods`, and `no-hardcoded-contract-id`. Exports a `recommended` config (ESLint v8 eslintrc) and `flat/recommended` config (ESLint v9 flat config).
