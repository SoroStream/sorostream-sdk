---
"@sorostream/sdk": minor
---

Add `filterStreams`/`sortStreams` helpers, SAC token metadata caching (`client.getTokenMetadata`/`clearTokenCache`), RPC-based network auto-detection, and structured transaction memo support (`WriteOptions.memo` + `parseMemo`).
